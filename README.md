# Электронная библиотека

АИС «Электронная библиотека» — учебный проект.  
Группа **241-326** | Крипак Ксения Романовна

---

## Запуск

### Через Docker (рекомендуется)

1. Скопировать файл с переменными окружения и при необходимости отредактировать:
   ```bash
   cp .env.example .env
   ```

2. Собрать образы и запустить все сервисы:
   ```bash
   docker compose up --build
   ```

Миграции применяются автоматически при старте контейнера.  
Приложение доступно по адресу: http://localhost:8000

Для запуска в фоновом режиме:
```bash
docker compose up -d --build
```

Остановка:
```bash
docker compose down
```

---

### Локальный запуск (без Docker)

Требуется запущенный PostgreSQL и заполненный `.env`.

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
flask run
```

Приложение доступно по адресу: http://127.0.0.1:5000

---

## Тестовые учётные записи

| Логин       | Пароль     | Роль           | ФИО                      |
|-------------|------------|----------------|--------------------------|
| `admin`     | `Admin1234`| Администратор  | Иванов Иван Иванович     |
| `moderator` | `Moder1234`| Модератор      | Петрова Мария Сергеевна  |
| `user`      | `User1234` | Пользователь   | Сидоров Алексей          |

---

## Стек

- **Backend:** Python 3, Flask, SQLAlchemy, Alembic
- **Frontend:** Jinja2, Bootstrap 5, EasyMDE
- **БД:** PostgreSQL 16 (Docker)
